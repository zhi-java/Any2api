services:
  omni:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: omni
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    environment:
      # DeepSeek 认证
      DS_TOKEN: ${DS_TOKEN:-}
      DS_TOKENS: ${DS_TOKENS:-}
      DS_ACCOUNTS: ${DS_ACCOUNTS:-}
      DS_ACCOUNTS_EXTENDED: ${DS_ACCOUNTS_EXTENDED:-}

      # 服务配置
      PORT: 3000
      API_KEY: ${API_KEY:-sk-zhi}
      API_KEYS: ${API_KEYS:-}

      # 代理配置
      HTTPS_PROXY: ${HTTPS_PROXY:-}
      HTTP_PROXY: ${HTTP_PROXY:-}

      # Token 池调优
      MAX_CONCURRENT_PER_TOKEN: ${MAX_CONCURRENT_PER_TOKEN:-2}
      TOKEN_DEAD_THRESHOLD: ${TOKEN_DEAD_THRESHOLD:-5}
      HEALTH_CHECK_INTERVAL: ${HEALTH_CHECK_INTERVAL:-600}
      IDLE_THRESHOLD: ${IDLE_THRESHOLD:-1800}

      # 会话管理
      SESSION_TTL: ${SESSION_TTL:-1800}
      MAX_REQUESTS_PER_SESSION: ${MAX_REQUESTS_PER_SESSION:-8}

      # 日志配置
      LOG_DIR: /app/logs
      CLIENT_DEBUG_LOG: ${CLIENT_DEBUG_LOG:-false}
      CLIENT_DEBUG_LOG_DIR: ${CLIENT_DEBUG_LOG_DIR:-}
      CLIENT_DEBUG_LOG_MAX_CHARS: ${CLIENT_DEBUG_LOG_MAX_CHARS:-200000}

      # 功能开关
      MERGE_THINKING: ${MERGE_THINKING:-false}
      ENABLE_PROMPT_INJECTION: ${ENABLE_PROMPT_INJECTION:-true}
      SYSTEM_FINGERPRINT: ${SYSTEM_FINGERPRINT:-fp_omni_v1}
      ENABLE_CONVERSATION_AFFINITY: ${ENABLE_CONVERSATION_AFFINITY:-false}
      CONVERSATION_TTL_MS: ${CONVERSATION_TTL_MS:-1800000}
      MAX_CONVERSATIONS: ${MAX_CONVERSATIONS:-500}
      MAX_TURNS_PER_SESSION: ${MAX_TURNS_PER_SESSION:-10}
      ENABLE_FC_ERROR_RETRY: ${ENABLE_FC_ERROR_RETRY:-true}
      FC_ERROR_RETRY_MAX_ATTEMPTS: ${FC_ERROR_RETRY_MAX_ATTEMPTS:-3}
      DEEPSEEK_CONTEXT_FALLBACK: ${DEEPSEEK_CONTEXT_FALLBACK:-true}
      DEEPSEEK_PRO_SAFE_INPUT_TOKENS: ${DEEPSEEK_PRO_SAFE_INPUT_TOKENS:-110000}

      # GLM 认证
      GLM_REFRESH_TOKEN: ${GLM_REFRESH_TOKEN:-}
      GLM_REFRESH_TOKENS: ${GLM_REFRESH_TOKENS:-}
      GLM_GUEST_MODE: ${GLM_GUEST_MODE:-true}

    volumes:
      # 持久化日志
      - ./logs:/app/logs
      # 如果需要自定义配置文件，可以挂载 .env
      # - ./.env:/app/.env:ro

    networks:
      - omni-network

    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  omni-network:
    driver: bridge

volumes:
  logs:
    driver: local

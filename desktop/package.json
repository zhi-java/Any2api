{
  "name": "omniapi-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "OmniAPI desktop shell (Tauri + tray console)",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "prepare:core": "node scripts/prepare-core.mjs",
    "prepare:core:force": "node scripts/prepare-core.mjs --force",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/plugin-autostart": "^2.5.1",
    "@tauri-apps/plugin-dialog": "^2.4.0",
    "@tauri-apps/plugin-notification": "^2.3.0",
    "@tauri-apps/plugin-shell": "^2.3.0"
  }
}
